node_modules/
dist/
.env
*.log
.DS_Store
gateway.json
pairings.json
codey-mac/pairings.json
workspaces/*
!workspaces/.gitkeep

*.env.local
.claude/
.codex/
*.local.json


# Codey Mac build artifacts
codey-mac/dist-electron/
codey-mac/release/
codey-mac/release-local/
# git worktrees
.worktrees/

# Intermediate iconset (icon.icns is committed)
codey-mac/build/icon.iconset/

# Private local docs — ignore everything under docs/ except the
# superpowers specs/plans, which we keep in version control.
docs/*
!docs/superpowers/
docs/superpowers/*
!docs/superpowers/specs/
!docs/superpowers/plans/

.codey/

*.tsbuildinfo

# Voice helper build artifacts (binary + bundle staged by `make helper`)
voice/CodeyVoice
voice/CodeyVoice.app/
voice/*.bin

# Stray compiled output that shouldn't sit at repo root
/gateway.js

.superpowers/

# Local tooling artifacts
graphify-out/
